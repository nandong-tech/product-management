# Header → Jira (agent)

**Not in use.** Header → **Jira** stays `—` unless the user gives a ticket. Do **not** fill PRD / Design / Implementation slots. Do **not** search the AIDR board to populate this field.

Ticket linking by slot can be added later. Until then, `publish-prd` does not change Header Jira except when the local PRD body is written to Confluence as a whole.

Board ids remain in [../config/confluence-projects.md](../config/confluence-projects.md) for that later work.
